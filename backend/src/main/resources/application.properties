# Server Configuration
server.port=${PORT:8080}

# PostgreSQL Database Configuration
# For Render: DATABASE_URL is provided automatically when you link a PostgreSQL database
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/safnectdb}
spring.datasource.driverClassName=org.postgresql.Driver
spring.datasource.username=${DATABASE_USERNAME:postgres}
spring.datasource.password=${DATABASE_PASSWORD:postgres}

# JPA Configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# CORS Configuration (handled by CorsConfig.java)
